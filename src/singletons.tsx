//import { Project } from "./services/project";

//const remote = window.require("electron").remote;
//eslint-disable-next-line
export const ProjectService = null;//remote.require('./services/project');
console.log(ProjectService);

export default {};
